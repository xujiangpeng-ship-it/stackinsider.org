{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">
{{- range .Data.Pages }}
{{- if and .Permalink (not .Draft) }}
{{- $isHome := .IsHome }}
{{- $isTaxonomy := or (eq .Kind "taxonomy") (eq .Kind "term") }}
{{- $isPost := and (eq .Kind "page") (not $isHome) (eq .Section "posts") }}
{{- $isStatic := and (eq .Kind "page") (not $isHome) (not $isPost) }}
  <url>
    <loc>{{ .Permalink }}</loc>{{ if not .Lastmod.IsZero }}
    <lastmod>{{ .Lastmod.Format "2006-01-02T15:04:05-07:00" }}</lastmod>{{ end }}
{{- if $isHome }}
    <changefreq>daily</changefreq>
    <priority>1.0</priority>
{{- else if $isTaxonomy }}
    <changefreq>weekly</changefreq>
    <priority>0.8</priority>
{{- else if $isStatic }}
    <changefreq>monthly</changefreq>
    <priority>0.5</priority>
{{- else if $isPost }}
{{- $thirtyDaysAgo := now.AddDate 0 0 -30 }}
{{- if .Lastmod.After $thirtyDaysAgo }}
    <changefreq>daily</changefreq>
{{- else }}
    <changefreq>weekly</changefreq>
{{- end }}
    <priority>0.7</priority>
{{- end }}
{{- if .IsTranslated }}{{ range .Translations }}
    <xhtml:link rel="alternate" hreflang="{{ .Language.Lang }}" href="{{ .Permalink }}"/>{{ end }}
    <xhtml:link rel="alternate" hreflang="{{ .Language.Lang }}" href="{{ .Permalink }}"/>{{ end }}
  </url>
{{- end }}
{{- end }}
</urlset>
